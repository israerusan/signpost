# Obsidian matches the release git tag to manifest.version exactly, with no "v"
# prefix. Make `npm version` create tags like `0.1.1`, not `v0.1.1`, so the
# release workflow's tag == manifest.version check passes.
tag-version-prefix=""
